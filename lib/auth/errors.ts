/** Generic messages — never reveal whether an email exists. */
export const AUTH_ERROR = {
  invalidCredentials: "Invalid email or password",
  tooManyAttempts: "Too many login attempts. Please try again later.",
} as const
